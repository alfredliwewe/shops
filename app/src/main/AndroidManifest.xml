<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rodz.shops">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shops">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" android:label="Home" />
        <activity android:name=".Products" android:label="Products" />
        <activity android:name=".Sales" android:label="Sales" />
        <activity android:name=".Shops" android:label="Shops" />
        <activity android:name=".Stock" android:label="Stock" />
        <activity android:name=".Reports" android:label="Reports" />
    </application>

</manifest>